# Markdown File

La cartella `Models/Contracts/Responses` contiene i modelli di risposta utilizzati per definire le strutture dei dati che vengono restituiti dal server tramite le API. Questi modelli rappresentano le informazioni che il client riceve in risposta alle richieste effettuate.