# Markdown File

La cartella "Controllers/V1" contiene i controller dell'API per la versione 1 dell'applicazione. I controller gestiscono le richieste HTTP, invocano i servizi di business appropriati e restituiscono le risposte al client.